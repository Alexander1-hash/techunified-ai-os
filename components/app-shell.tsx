'use client';
import Link from 'next/link'; import {usePathname} from 'next/navigation'; import {Menu, Bell, Search, Plus, Sparkles, ChevronDown, LogOut} from 'lucide-react'; import {nav} from '@/lib/data'; import {createClient} from '@/lib/supabase/client'; import {useAuth} from '@/components/auth-provider';
export function AppShell({children}:{children:React.ReactNode}){const path=usePathname(); const {user,profile,organization,role}=useAuth(); if(path==='/login'||path.startsWith('/auth')||path==='/about'||path.startsWith('/founder')) return <>{children}</>; async function signOut(){await createClient().auth.signOut(); window.location.href='/login'} return <div className="min-h-screen bg-background"><aside className="fixed inset-y-0 left-0 z-30 hidden w-64 border-r bg-[#09151e] lg:flex lg:flex-col"><div className="flex h-20 items-center gap-3 border-b px-6"><div className="flex size-9 items-center justify-center rounded-xl bg-primary text-primary-foreground"><Sparkles size={18}/></div><div><div className="font-semibold tracking-tight">TECHUNIFIED</div><div className="text-[10px] uppercase tracking-[.24em] text-muted-foreground">AI OS</div></div></div><nav className="flex flex-1 flex-col gap-1 p-4">{nav.map(([label,href,icon])=><Link key={href} href={href} className={`flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm transition ${path===href||path.startsWith(href+'/')?'bg-primary/10 text-primary':'text-muted-foreground hover:bg-muted hover:text-foreground'}`}><span className="w-5 text-center text-base">{icon}</span>{label}</Link>)}</nav><div className="border-t p-4"><div className="mb-3 rounded-lg bg-muted/60 p-3"><div className="text-xs text-muted-foreground">Workspace</div><div className="mt-1 flex items-center justify-between text-sm font-medium">TechUnified <ChevronDown size={14}/></div><div className="mt-2 text-xs text-primary">Scale plan</div></div><div className="flex items-center gap-3"><div className="flex size-9 items-center justify-center rounded-full bg-accent font-semibold text-[#182016]">AS</div><div className="min-w-0"><div className="truncate text-sm font-medium">{String(profile?.full_name??user?.user_metadata?.full_name??'Alexander Smith')}</div><div className="truncate text-xs text-muted-foreground">{role} · {String(organization?.name??'TechUnified')}</div></div><button onClick={signOut} aria-label="Sign out" className="ml-auto rounded-md p-2 text-muted-foreground hover:bg-muted hover:text-foreground"><LogOut size={15}/></button></div></div></aside><div className="lg:pl-64"><header className="sticky top-0 z-20 flex h-16 items-center justify-between border-b bg-[#071019]/90 px-4 backdrop-blur md:px-8"><div className="flex items-center gap-3"><button className="rounded-lg p-2 hover:bg-muted lg:hidden" aria-label="Open navigation"><Menu size={20}/></button><div className="hidden items-center gap-2 rounded-lg bg-muted px-3 py-2 text-sm text-muted-foreground md:flex"><Search size={15}/> Search workspace <kbd className="ml-4 rounded border px-1.5 text-[10px]">⌘ K</kbd></div><span className="text-sm text-muted-foreground md:hidden">TechUnified <span className="text-primary">/</span> OS</span></div><div className="flex items-center gap-2"><button className="rounded-lg p-2 text-muted-foreground hover:bg-muted hover:text-foreground" aria-label="Notifications"><Bell size={18}/></button><button className="hidden items-center gap-2 rounded-lg bg-primary px-3 py-2 text-sm font-medium text-primary-foreground hover:opacity-90 sm:flex"><Plus size={16}/> Quick action</button><div className="flex size-9 items-center justify-center rounded-full bg-accent text-sm font-semibold text-[#182016]">AS</div></div></header><main className="min-h-[calc(100vh-4rem)] px-4 py-6 pb-24 md:px-8 lg:px-10">{children}</main></div><nav className="fixed inset-x-0 bottom-0 z-30 flex h-16 items-center justify-around border-t bg-[#09151e]/95 px-2 backdrop-blur lg:hidden">{nav.slice(0,5).map(([label,href,icon])=><Link key={href} href={href} className={`flex flex-col items-center gap-1 px-2 py-1 text-[10px] ${path.startsWith(href)?'text-primary':'text-muted-foreground'}`}><span className="text-base">{icon}</span>{label==='Company Brain'?'Brain':label}</Link>)}</nav></div>}
