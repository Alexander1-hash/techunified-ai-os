import {NextResponse} from 'next/server'
import {createClient} from '@/lib/supabase/server'
import {createAdminClient} from '@/lib/supabase/admin'
import {creditCost,models} from '@/lib/ai/types'
import {enqueueGeneration} from '@/lib/ai/inference'
export async function POST(request:Request){const supabase=await createClient(); const {data:{user}}=await supabase.auth.getUser(); if(!user)return NextResponse.json({error:'unauthorized'},{status:401}); const body=await request.json().catch(()=>null); if(!body?.prompt||typeof body.prompt!=='string'||body.prompt.trim().length<3)return NextResponse.json({error:'Describe your generation in at least 3 characters.'},{status:400}); const type=body.type==='image'?'image':'video'; const model=models.find(m=>m.type===type)?.id??(type==='image'?'our-image-model':'our-video-model'); const requestData={type,mode:body.mode??(type==='image'?'text-to-image':'text-to-video'),prompt:body.prompt.trim().slice(0,4000),model,aspectRatio:body.aspectRatio??'16:9',duration:type==='video'?(body.duration===10?10:5):undefined,quality:body.quality==='high'?'high':'standard',idempotencyKey:typeof body.idempotencyKey==='string'?body.idempotencyKey.slice(0,120):crypto.randomUUID()}; const cost=creditCost(requestData as never); const {data:existing}=await supabase.from('generations').select('id,status').eq('user_id',user.id).eq('idempotency_key',requestData.idempotencyKey).maybeSingle(); if(existing)return NextResponse.json(existing); const {data:credit}=await supabase.from('user_credits').select('balance').eq('user_id',user.id).maybeSingle(); if((credit?.balance??0)<cost)return NextResponse.json({error:'Not enough credits to generate this video.'},{status:402}); const admin=createAdminClient(); const {data:reserved}=await admin.from('user_credits').update({balance:(credit?.balance??0)-cost,updated_at:new Date().toISOString()}).eq('user_id',user.id).gte('balance',cost).select('user_id').maybeSingle(); if(!reserved)return NextResponse.json({error:'Not enough credits to generate this video.'},{status:402}); const {data:generation,error}=await supabase.from('generations').insert({user_id:user.id,...requestData,credits_reserved:cost,provider:process.env.AI_WORKER_URL?'worker':'mock'}).select('id,status,credits_reserved').single(); if(error)return NextResponse.json({error:'Could not create generation.'},{status:500}); await enqueueGeneration({id:generation.id,userId:user.id,request:requestData as never}); return NextResponse.json({generationId:generation.id,status:generation.status,credits:cost,developmentMode:!process.env.AI_WORKER_URL},{status:202})}
